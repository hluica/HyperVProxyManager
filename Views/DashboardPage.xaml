<UserControl
    x:Class="HyperVProxyManager.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:HyperVProxyManager.ViewModels">

    <behaviors:Interaction.Triggers>
        <behaviors:KeyTrigger Key="F5">
            <behaviors:InvokeCommandAction Command="{Binding RefreshCommand}" />
        </behaviors:KeyTrigger>
        <behaviors:EventTrigger EventName="Loaded">
            <behaviors:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </behaviors:EventTrigger>
    </behaviors:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  主内容区域  -->
        <Grid
            Grid.Row="0"
            Margin="24,12,24,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="24" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="24" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--  Info Cards  -->
            <ui:Card
                Grid.Row="0"
                Grid.Column="0"
                VerticalAlignment="Stretch">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <ui:SymbolIcon
                        Grid.Row="0"
                        HorizontalAlignment="Left"
                        FontSize="24"
                        Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                        Symbol="Desktop24" />

                    <TextBlock
                        Grid.Row="1"
                        Margin="0,12,0,4"
                        Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                        Text="宿主机 IPv4" />

                    <Grid
                        Grid.Row="2"
                        HorizontalAlignment="Left">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            FontSize="24"
                            FontWeight="SemiBold"
                            Text="{Binding HostIpAddress}"
                            TextWrapping="Wrap" />

                        <ui:Button
                            Grid.Column="1"
                            Margin="8,0,0,0"
                            Padding="6"
                            VerticalAlignment="Center"
                            Appearance="Transparent"
                            Click="OnCopyContentButtonClick"
                            Tag="{Binding HostIpAddress}">
                            <ui:SymbolIcon
                                FontSize="20"
                                Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                                Symbol="Copy24" />
                        </ui:Button>
                    </Grid>
                </Grid>
            </ui:Card>

            <ui:Card
                Grid.Row="0"
                Grid.Column="2"
                VerticalAlignment="Stretch">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel
                        Grid.Row="0"
                        Orientation="Horizontal">
                        <ui:SymbolIcon
                            FontSize="24"
                            Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                            Symbol="Globe24" />

                        <Ellipse
                            Width="12"
                            Height="12"
                            Margin="12,0,12,0"
                            VerticalAlignment="Center">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="#737373" />
                                    <Style.Triggers>
                                        <DataTrigger
                                            Binding="{Binding IsProxyEnabled}"
                                            Value="True">
                                            <Setter Property="Fill" Value="#00CC6A" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                    </StackPanel>

                    <TextBlock
                        Grid.Row="1"
                        Margin="0,12,0,4"
                        Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                        Text="当前系统代理" />

                    <Grid
                        Grid.Row="2"
                        HorizontalAlignment="Left">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            FontSize="24"
                            FontWeight="SemiBold"
                            Text="{Binding CurrentProxyAddress}"
                            TextWrapping="Wrap" />

                        <ui:Button
                            Grid.Column="1"
                            Margin="8,0,0,0"
                            Padding="6"
                            VerticalAlignment="Center"
                            Appearance="Transparent"
                            Click="OnCopyContentButtonClick"
                            Tag="{Binding CurrentProxyAddress}">
                            <ui:SymbolIcon
                                FontSize="20"
                                Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                                Symbol="Copy24" />
                        </ui:Button>
                    </Grid>
                </Grid>
            </ui:Card>

            <!--  Buttons  -->
            <ui:Button
                Grid.Row="2"
                Grid.Column="0"
                Height="50"
                HorizontalAlignment="Stretch"
                Appearance="Primary"
                Command="{Binding QuickSetCommand}"
                IsEnabled="{Binding CanQuickSet}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <ui:SymbolIcon
                        Grid.Column="0"
                        FontSize="18"
                        Symbol="Rocket24" />

                    <TextBlock
                        Grid.Column="1"
                        Margin="10,0,0,0"
                        VerticalAlignment="Center"
                        FontSize="14">
                        <Run Text="应用宿主机 IP" />
                        <Run Text=" (" />
                        <Run Text="{Binding TargetPort, Mode=OneWay}" />
                        <Run Text=")" />
                    </TextBlock>
                </Grid>
            </ui:Button>

            <Grid
                Grid.Row="2"
                Grid.Column="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ui:Button
                    Grid.Column="0"
                    Height="50"
                    HorizontalAlignment="Stretch"
                    Appearance="Secondary"
                    Command="{Binding RefreshCommand}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <ui:SymbolIcon
                            Grid.Column="0"
                            FontSize="18"
                            Symbol="ArrowCounterclockwise24" />

                        <TextBlock
                            Grid.Column="1"
                            Margin="8,0,0,0"
                            VerticalAlignment="Center"
                            Text="刷新" />
                    </Grid>
                </ui:Button>

                <ui:Button
                    Grid.Column="2"
                    Height="50"
                    HorizontalAlignment="Stretch"
                    Appearance="Danger"
                    Command="{Binding DisableProxyCommand}"
                    IsEnabled="{Binding IsProxyEnabled}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <ui:SymbolIcon
                            Grid.Column="0"
                            FontSize="18"
                            Symbol="DismissCircle24" />

                        <TextBlock
                            Grid.Column="1"
                            Margin="8,0,0,0"
                            VerticalAlignment="Center"
                            Text="禁用" />
                    </Grid>
                </ui:Button>
            </Grid>
        </Grid>

        <!--  StatusBar  -->
        <StatusBar
            Grid.Row="1"
            Padding="12,8"
            Background="Transparent">
            <StatusBarItem>
                <TextBlock
                    FontSize="12"
                    Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                    Text="{Binding StatusMessage}" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>